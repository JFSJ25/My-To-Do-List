import {
  SortOption,
  type SortOption as SortOptionType
} from '../types/sortOption'

export const SORT_OPTIONS: SortOptionType[] = [
  SortOption.custom,
  SortOption.priority,
  SortOption.date
]
